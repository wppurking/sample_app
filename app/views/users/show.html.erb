<% provide(:title, @user.name) %>
<div class="row">
  <!-- aside 与 section 都是 html5 标签 -->
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user, size: 100 %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
