<% if @feed_items.any? %>
  <ol class="microposts">
    <!-- 这里不直接使用 render @feed_items 的原因是, 没有这个 model ,只能指明需要的页面 -->
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>